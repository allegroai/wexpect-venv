#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    # setup_requires=['pbr'],
    # pbr=True,
    
    # packages=[''],
    #py_modules=['wexpect'],
    packages=find_packages()
)